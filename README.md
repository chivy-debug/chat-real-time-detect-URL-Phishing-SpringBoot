# chat-real-time-detect-URL-Phishing-SpringBoot

Run the flask_api.py file first to load the AI ​​model, the program uses the API to exchange the URL extracted from the conversation.

Usage
------
1. python flask_api.py
2. run the Spring Boot project and access http://localhost:8080/
